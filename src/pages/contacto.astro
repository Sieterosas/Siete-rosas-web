---
import BaseLayout from "../layouts/BaseLayout.astro";
---

<BaseLayout
  title="Contacto | Sieterosas"
  description="Ponte en contacto para reservar tu reportaje o solicitar más información."
>
  <section class="max-w-4xl mx-auto px-6 py-16">
    <h1 class="text-3xl md:text-4xl font-serif text-gray-900 mb-6">
      Contacto
    </h1>
    <p class="text-gray-700 mb-8">
      Si quieres más información sobre nuestros reportajes de boda, comuniones, newborn o eventos,
      rellena el formulario y te responderemos lo antes posible.
    </p>

    <form class="grid grid-cols-1 gap-6">
      <input
        type="text"
        name="name"
        placeholder="Tu nombre"
        required
        class="border border-gray-300 rounded-lg px-4 py-3 focus:ring-2 focus:ring-gray-600"
      />

      <input
        type="email"
        name="email"
        placeholder="Tu email"
        required
        class="border border-gray-300 rounded-lg px-4 py-3 focus:ring-2 focus:ring-gray-600"
      />

      <textarea
        name="message"
        rows="5"
        placeholder="Cuéntanos tu idea..."
        required
        class="border border-gray-300 rounded-lg px-4 py-3 focus:ring-2 focus:ring-gray-600"
      ></textarea>

      <button
        type="submit"
        class="bg-gray-900 text-white py-3 px-6 rounded-lg hover:bg-gray-700 transition"
      >
        Enviar
      </button>
    </form>
  </section>
</BaseLayout>